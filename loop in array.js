const number = [15 ,25, 35, 45, 55, 65, 75];
for(let i = 0; i<number.length; i++){ // i<=number.length  cz index 0 
    // but elemnt 0 er theke 1 ti besi.//
    const element = number[i];   //i=1,2,3,4,5....
    console.log(element);
}